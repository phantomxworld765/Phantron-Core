# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# PHANTRON runtime output
*.mp3
screenshot_*.png

# OS / editor
.DS_Store
Thumbs.db
*.log

# Secrets — never commit your real API keys.
# config.json holds your private keys (OpenRouter / Claude). It stays LOCAL.
# Use config.example.json as the shareable template.
config.json
*.bak
