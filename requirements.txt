# ── PHANTRON v7 CORE dependencies ───────────────────────────
# These ALL ship prebuilt wheels, so they install cleanly on Windows
# without any C/C++ compiler. PHANTRON's interface, metrics, screen feed,
# mouse/keyboard, screenshots and app control all work with just these.
#
#   pip install -r requirements.txt
#
# Voice (mic input / spoken output) is OPTIONAL and lives in
# requirements-voice.txt so a failed build there can never break the core.

websockets>=11.0
psutil>=5.9.0
requests>=2.31.0
pyautogui>=0.9.54
Pillow>=10.0.0
