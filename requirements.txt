# ─────────────────────────────────────────────────────────────
#  PHANTRON v2 dependencies
#
#  THE CORE NEEDS NOTHING FROM THIS FILE.
#  `python -m phantron` boots on the Python standard library alone:
#  web UI, chat, app launching, web search, files and shell all work.
#
#  Install the extras below ONLY for the features you want. Each one is
#  optional and PHANTRON skips it gracefully if it is missing.
# ─────────────────────────────────────────────────────────────

# --- System info (CPU / RAM / battery) ---
psutil>=5.9.0

# --- Screen + mouse/keyboard automation, screenshots ---
pyautogui>=0.9.54
Pillow>=10.0.0

# --- Voice INPUT (microphone -> text) ---
SpeechRecognition>=3.10.0
# PyAudio drives the mic. On Windows, if it fails to build:
#   pip install pipwin && pipwin install pyaudio
pyaudio>=0.2.13

# --- Voice OUTPUT (text -> speech), offline + cross-platform ---
pyttsx3>=2.90
# Windows-only native voices (optional, good Hindi voice):
pywin32>=306 ; sys_platform == "win32"

# NOTE: the "brain" needs NO python package.
#   * Local + free : install Ollama from https://ollama.com (separate app)
#   * Cloud        : just put an API key in config.json (uses urllib directly)
